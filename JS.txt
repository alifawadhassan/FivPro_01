window.onhashchange = function() {
    if (window.innerDocClick) {
        
    } else {
        		
		setTimeout(function () {
        window.scrollTo( 0, 1230 );
    }, 100);
		
		
		window.onhashchange = function() {
    if (window.innerDocClick) {
     } else {
        		
		setTimeout(function () {
        window.scrollTo( 0, 1230 );
    }, 100);
		
    }
}
		
		
		
    }
}